# Spotify OAuth client ID. Leave empty to use the bundled librespot default
# (which only allows redirect to http://127.0.0.1:3001/callback).
# To use your own Spotify dev app, set this to its client ID and register
# http://127.0.0.1:3001/callback as a redirect URI in the Spotify dashboard.
SPOTIFY_CLIENT_ID=
